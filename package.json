{
  "name": "pi-adam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal pi quality-of-life extension.",
  "keywords": ["pi-package"],
  "scripts": {
    "test": "node --test features/*.test.ts herdr-plugin/*.test.js"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
